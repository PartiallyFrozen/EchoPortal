# Optional: the voice face (tap to talk, transcribed locally, typed into the focused window).
#
# faster-whisper pulls in CTranslate2. For GPU transcription you also need CUDA 12 and cuDNN 9 on
# the PATH; on CPU it works but a sentence takes seconds rather than a moment. The model itself is
# downloaded on first use into the Hugging Face cache, not shipped with the installer.

-r requirements.txt
faster-whisper>=1.0
