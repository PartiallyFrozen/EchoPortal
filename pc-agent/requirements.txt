# EchoPortal agent, runtime dependencies (Windows 10/11, Python 3.12).
# Voice is optional and lives in requirements-voice.txt.

websockets>=12.0        # the hub the Spot connects to
aiohttp>=3.14.3            # media server, ComfyUI and weather HTTP
psutil>=5.9             # CPU, memory, disks, processes
pywin32>=306            # performance counters, focused-window typing, clipboard
nvidia-ml-py>=12.535    # GPU name, temperature, power (import pynvml)
pystray>=0.19           # system tray icon
pillow>=10.0            # tray icon, thumbnails, weather art crops
numpy>=1.26             # audio level maths for the voice face
